// C# 言語機能で追加された union 型。
//
// 「type union」って言われてるもので、
// 要は「object に値を突っ込んで、isinst (.NET IL の型チェック命令)で中身確認するタイプの実装。
//
// struct U { public object? Value { get; } が作られてる。
//
// int とかを使うと普通にボックス化起こす。
// 全部が参照型なら問題ないんだけども。
//
// さらに言うと U u = default; object obj = u; するとこれもさらにボックス化する。
// 正直、「ユーザー定義の object 変換」みたいなの(を使ってボックス化回避)がないとやってられないというお気持ちちょっとある。
//
// だいぶ嫌な仕様…
// 叩かれてるけど変わらなさそう。
// DU.cs みたいな「自作ならボックス化避けれるから自作しろ」路線か、別構文が今後入るか。
union U(string x, Uri y);
